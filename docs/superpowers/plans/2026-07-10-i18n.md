# i18n (es/en) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Todo el output del CLI (contenido generado, prompts, mensajes) disponible en español e inglés, con auto-detección de locale y flag `--lang es|en`.

**Architecture:** Módulo `core/i18n.ts` con los patrones de texto compartidos como funciones bilingües y los textos de interfaz en tablas `Record<Lang, ...>`. Los 15 packs reciben `lang` en `rules`/`promptTemplates` y conservan solo fragmentos específicos en tablas locales `{ es, en }`. Spec: `docs/superpowers/specs/2026-07-10-i18n-design.md`.

**Tech Stack:** TypeScript ESM, vitest.

## Global Constraints

- TDD estricto; suite: `npm test --workspace=packages/cli -- run <archivo>`.
- `lang` es obligatorio en packs y renders; opcional solo en `runCli` (default `detectLang()`).
- Los fragmentos específicos de cada pack se traducen fielmente a partir del texto español existente en el propio archivo; los patrones compartidos SIEMPRE via i18n (nunca duplicar la plantilla en un pack).
- Los títulos de PromptTemplate no cambian.
- Commits `feat:`/`docs:` con footer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; mensajes multilínea via `git commit -F`.

---

### Task 1: Módulo `core/i18n.ts`

**Files:**
- Create: `packages/cli/src/core/i18n.ts`
- Test: `packages/cli/test/i18n.test.ts`

**Interfaces (produce):**

```typescript
export type Lang = "es" | "en";
export function detectLang(): Lang;
export function summarySentence(lang: Lang, language: string, framework?: string, parenthetical?: string): string;
export function runTestsConvention(lang: Lang, cmd: string): string;
export function reviewBody(lang: Lang, focus: string, framework: string): string;
export function refactorBody(lang: Lang, extra?: string): string;
export function testingBody(lang: Lang, runner: string): string;
export function unknownRunnerLabel(lang: Lang): string;
export function unknownFrameworkLabel(lang: Lang): string;
export const UI: Record<Lang, UiTexts>; // ver Step 3
```

- [ ] **Step 1: Test rojo** (`test/i18n.test.ts`):

```typescript
import { describe, it, expect, vi, afterEach } from "vitest";
import {
  detectLang, summarySentence, runTestsConvention, reviewBody,
  refactorBody, testingBody, unknownRunnerLabel, UI,
} from "../src/core/i18n.js";

afterEach(() => vi.restoreAllMocks());

describe("detectLang", () => {
  it("returns es for Spanish locales and en otherwise", () => {
    const spy = vi.spyOn(Intl, "DateTimeFormat");
    spy.mockReturnValue({ resolvedOptions: () => ({ locale: "es-MX" }) } as never);
    expect(detectLang()).toBe("es");
    spy.mockReturnValue({ resolvedOptions: () => ({ locale: "de-DE" }) } as never);
    expect(detectLang()).toBe("en");
    spy.mockImplementation(() => { throw new Error("boom"); });
    expect(detectLang()).toBe("en");
  });
});

describe("shared builders", () => {
  it("builds the summary in both languages", () => {
    expect(summarySentence("es", "Python", "flask")).toBe("Proyecto Python con flask.");
    expect(summarySentence("en", "Python", "flask")).toBe("Python project using flask.");
    expect(summarySentence("es", "Go", "gin", "go modules")).toBe("Proyecto Go con gin (go modules).");
    expect(summarySentence("en", "Go", undefined, "go modules")).toBe("Go project (go modules).");
    expect(summarySentence("es", "Python")).toBe("Proyecto Python.");
  });

  it("builds conventions and prompt bodies in both languages", () => {
    expect(runTestsConvention("es", "pytest")).toBe("Ejecuta los tests con pytest antes de terminar una tarea.");
    expect(runTestsConvention("en", "pytest")).toBe("Run the tests with pytest before finishing a task.");
    expect(reviewBody("en", "incorrect exception handling", "flask")).toContain("incorrect exception handling");
    expect(reviewBody("es", "manejo de excepciones incorrecto", "flask")).toContain("Señala solo problemas concretos");
    expect(refactorBody("en")).toContain("without changing observable behavior");
    expect(testingBody("es", "pytest")).toContain("camino feliz");
    expect(testingBody("en", "pytest")).toContain("happy path");
    expect(unknownRunnerLabel("en")).toBe("the project's test runner");
  });

  it("exposes UI texts for both languages", () => {
    expect(UI.es.sections.commands).toBe("Comandos del repo");
    expect(UI.en.sections.commands).toBe("Repo commands");
    expect(UI.en.generatedHeader).toContain("Generated by agent-rules-init");
    expect(UI.es.question("el framework", "Python")).toContain("¿Cuál usáis?");
    expect(UI.en.question("the framework", "Python")).toContain("Which one do you use?");
    expect(UI.en.fieldLabels.testRunner).toBe("the test runner");
    expect(UI.es.fieldLabels.packageManager).toBe("el gestor de paquetes");
  });
});
```

- [ ] **Step 2: Verificar rojo** (módulo inexistente).
- [ ] **Step 3: Implementación** — `src/core/i18n.ts`:

```typescript
export type Lang = "es" | "en";

export function detectLang(): Lang {
  try {
    const locale = Intl.DateTimeFormat().resolvedOptions().locale ?? "";
    return locale.toLowerCase().startsWith("es") ? "es" : "en";
  } catch {
    return "en";
  }
}

export function summarySentence(lang: Lang, language: string, framework?: string, parenthetical?: string): string {
  const paren = parenthetical ? ` (${parenthetical})` : "";
  if (lang === "es") return `Proyecto ${language}${framework ? ` con ${framework}` : ""}${paren}.`;
  return `${language} project${framework ? ` using ${framework}` : ""}${paren}.`;
}

export function runTestsConvention(lang: Lang, cmd: string): string {
  return lang === "es"
    ? `Ejecuta los tests con ${cmd} antes de terminar una tarea.`
    : `Run the tests with ${cmd} before finishing a task.`;
}

export function reviewBody(lang: Lang, focus: string, framework: string): string {
  return lang === "es"
    ? `Revisa el diff actual buscando bugs, ${focus} y desviaciones de las convenciones de ${framework}. Señala solo problemas concretos con línea de archivo.`
    : `Review the current diff looking for bugs, ${focus} and deviations from ${framework} conventions. Point out only concrete issues with file and line.`;
}

export function refactorBody(lang: Lang, extra?: string): string {
  const base =
    lang === "es"
      ? "Propón refactors que reduzcan duplicación y mejoren la legibilidad sin cambiar comportamiento observable."
      : "Propose refactors that reduce duplication and improve readability without changing observable behavior.";
  return extra ? `${base} ${extra}` : base;
}

export function testingBody(lang: Lang, runner: string): string {
  return lang === "es"
    ? `Escribe tests con ${runner} para el código señalado. Cubre el camino feliz y al menos un caso límite.`
    : `Write tests with ${runner} for the highlighted code. Cover the happy path and at least one edge case.`;
}

export function unknownRunnerLabel(lang: Lang): string {
  return lang === "es" ? "el test runner del proyecto" : "the project's test runner";
}

export function unknownFrameworkLabel(lang: Lang): string {
  return lang === "es" ? "el framework del proyecto" : "the project's framework";
}

export interface UiTexts {
  generatedHeader: string;
  sections: { commands: string; structure: string; ci: string };
  andMore: (count: number, file?: string) => string;
  noStackFallback: string;
  question: (fieldLabel: string, language: string) => string;
  fieldLabels: { framework: string; testRunner: string; linter: string; packageManager: string };
  usage: string;
  unknownOption: (flag: string) => string;
  invalidLang: (value: string) => string;
  noTtyWarning: string;
  skippedQuestion: (message: string) => string;
  polishDetected: (assistant: string) => string;
  polishConfirm: (assistant: string) => string;
  polishFailed: (assistant: string, error: string) => string;
  polishPrompt: (content: string) => string;
  fileSkipped: (path: string) => string;
  outroWritten: string;
  outroNothing: string;
  unexpectedError: (message: string) => string;
  dirNotes: Record<string, string>;
}

export const UI: Record<Lang, UiTexts> = { es: { /* textos actuales movidos aquí */ }, en: { /* traducciones */ } };
```

El contenido exacto de `UI.es` son los strings hoy repartidos por `cli.ts`,
`templates.ts`, `prompt-engine.ts`, `llm-bridge.ts` y el `DIR_NOTES` de
`repo-facts.ts` (moverlos, no reescribirlos). `UI.en` es su traducción fiel.
`dirNotes` reemplaza a `DIR_NOTES` (mismas claves; valores por idioma:
"código fuente"/"source code", "tests"/"tests", "documentación"/"documentation",
"ejemplos"/"examples", "scripts auxiliares"/"helper scripts", "herramientas
auxiliares"/"helper tooling", "migraciones de base de datos"/"database
migrations", "benchmarks"/"benchmarks", "workflows y configuración de
GitHub"/"GitHub workflows and configuration", "activos públicos"/"public
assets", "activos estáticos"/"static assets", "activos"/"assets",
"configuración"/"configuration").

- [ ] **Step 4: Verificar verde.**
- [ ] **Step 5: Commit** — `feat(i18n): add language detection and bilingual text builders`.

---

### Task 2: `--lang` en `resolveCliAction`

**Files:**
- Modify: `packages/cli/src/cli.ts` (solo `resolveCliAction` y tipo `CliAction`)
- Test: `packages/cli/test/cli.test.ts`

**Interfaces:**
- Produces: `CliAction = { kind: "run"; lang?: Lang } | { kind: "help" } | { kind: "version" } | { kind: "invalid-lang"; value: string } | { kind: "unknown"; flag: string }`.

- [ ] **Step 1: Test rojo** (en el describe de `resolveCliAction`):

```typescript
it("parses --lang with space and = forms", () => {
  expect(resolveCliAction(["--lang", "en"])).toEqual({ kind: "run", lang: "en" });
  expect(resolveCliAction(["--lang=es"])).toEqual({ kind: "run", lang: "es" });
});

it("rejects an invalid --lang value", () => {
  expect(resolveCliAction(["--lang", "fr"])).toEqual({ kind: "invalid-lang", value: "fr" });
  expect(resolveCliAction(["--lang"])).toEqual({ kind: "invalid-lang", value: "" });
});
```

- [ ] **Step 2: Verificar rojo.**
- [ ] **Step 3: Implementación** — reescribir `resolveCliAction` como bucle sobre argv: `--help|-h` → help (prioridad), `--version|-v` → version, `--lang <v>`/`--lang=<v>` → acumula lang si `v` es `es|en`, si no `invalid-lang`; cualquier otro token → `unknown`. Sin flags → `{ kind: "run" }`.
- [ ] **Step 4: Verificar verde** (`test/cli.test.ts`).
- [ ] **Step 5: Commit** — `feat(cli): parse --lang flag`.

---

### Task 3: Contrato Pack + refactor de los 15 packs

**Files:**
- Modify: `packages/cli/src/core/types.ts` (firma de `Pack`)
- Modify: `packages/cli/src/packs/*.ts` (los 15)
- Test: `packages/cli/test/packs/*.test.ts` (los 15)

**Interfaces:**
- Produces: `Pack.rules(detection: DetectionResult, lang: Lang): RuleSet` y `Pack.promptTemplates(detection: DetectionResult, lang: Lang): PromptTemplate[]`.

Mecánica por pack (ejemplo completo con python; los demás siguen el mismo molde):

```typescript
// python.ts (rules y promptTemplates tras el refactor)
import { reviewBody, refactorBody, runTestsConvention, summarySentence, testingBody, unknownFrameworkLabel, unknownRunnerLabel, type Lang } from "../core/i18n.js";

const TEXTS: Record<Lang, { style: string; deps: string; arch: string[]; reviewFocus: string; refactorExtra: string }> = {
  es: {
    style: "Sigue PEP 8; usa type hints en funciones públicas.",
    deps: "No introduzcas dependencias nuevas sin añadirlas al manifiesto de dependencias existente.",
    arch: [
      "Mantén la lógica de negocio separada de la capa de framework cuando el proyecto ya siga ese patrón.",
      "Usa entornos virtuales; no instales paquetes globalmente.",
    ],
    reviewFocus: "manejo de excepciones incorrecto",
    refactorExtra: "Respeta los type hints existentes.",
  },
  en: {
    style: "Follow PEP 8; use type hints on public functions.",
    deps: "Do not introduce new dependencies without adding them to the existing dependency manifest.",
    arch: [
      "Keep business logic separate from the framework layer when the project already follows that pattern.",
      "Use virtual environments; never install packages globally.",
    ],
    reviewFocus: "incorrect exception handling",
    refactorExtra: "Respect the existing type hints.",
  },
};

function rules(detection: DetectionResult, lang: Lang): RuleSet {
  const t = TEXTS[lang];
  const framework = detection.framework?.value;
  const runner = detection.testRunner?.value !== "unknown" ? detection.testRunner?.value : undefined;
  return {
    summary: summarySentence(lang, "Python", framework !== "none" ? framework : undefined),
    conventions: [t.style, runTestsConvention(lang, runner ?? unknownRunnerLabel(lang)), t.deps],
    architectureNotes: t.arch,
  };
}

function promptTemplates(detection: DetectionResult, lang: Lang): PromptTemplate[] {
  const t = TEXTS[lang];
  const framework = detection.framework?.value !== "none" ? detection.framework!.value : unknownFrameworkLabel(lang);
  const runner = detection.testRunner?.value !== "unknown" ? detection.testRunner!.value : unknownRunnerLabel(lang);
  return [
    { id: "review", title: "Code Review (Python)", body: reviewBody(lang, t.reviewFocus, framework) },
    { id: "refactor", title: "Refactor (Python)", body: refactorBody(lang, t.refactorExtra) },
    { id: "testing", title: "Testing (Python)", body: testingBody(lang, runner) },
  ];
}
```

Reglas para los otros 14: (a) el texto español existente del archivo se mueve
a `TEXTS.es` tal cual (los guards `!== "unknown"` se sustituyen por el patrón
`runner`/`framework` de arriba); (b) `TEXTS.en` es traducción fiel; (c) las
frases que coinciden con un patrón compartido se sustituyen por la llamada
i18n (aunque la redacción actual varíe ligeramente entre packs —
"usando"/"con", "framework de tests"/"test runner" — se normaliza al patrón
compartido); (d) los comandos con formato específico ("bundle exec rspec",
"cmake --build . && ctest", "mvn test"/"gradle test") se conservan calculados
igual que hoy y se pasan como `cmd` a `runTestsConvention`; (e) convenciones
extra que hoy existen (p. ej. módulos ES/CommonJS en js-ts, TypeScript
estricto) van a `TEXTS` con su traducción.

- [ ] **Step 1: Test rojo** — actualizar los 15 `test/packs/*.test.ts`: cada llamada `pack.rules(detection)` pasa a `pack.rules(detection, "es")` (asserts intactos) y cada `promptTemplates(detection)` a `promptTemplates(detection, "es")`; añadir a cada archivo un test en inglés:

```typescript
it("renders rules and templates in English when lang is en", () => {
  const detection = <detección de alta confianza ya usada en el archivo>!;
  const ruleSet = <pack>.rules(detection, "en");
  expect(ruleSet.summary).toContain("project");
  expect(ruleSet.summary).not.toContain("Proyecto");
  const testing = <pack>.promptTemplates(detection, "en").find((t) => t.id === "testing")!;
  expect(testing.body).toContain("happy path");
});
```

- [ ] **Step 2: Verificar rojo** — los tests en inglés fallan (y TypeScript aún compila el contrato viejo; el rojo esperado es fallo de assert o de firma).
- [ ] **Step 3: Implementación** — cambiar la firma en `types.ts` y refactorizar los 15 packs con el molde de arriba, en tandas (python/js-ts/go, luego java/php/ruby/rust/csharp, luego kotlin/swift/dart/cpp/elixir/scala/r), verificando `npm test --workspace=packages/cli -- run test/packs/` tras cada tanda.
- [ ] **Step 4: Verificar verde** — todos los tests de packs.
- [ ] **Step 5: Commit** — `feat(packs): bilingual rules and prompt templates via shared i18n patterns`.

---

### Task 4: repo-facts y templates con `lang`

**Files:**
- Modify: `packages/cli/src/core/repo-facts.ts` (`extractStructure(signals, lang)`, `buildRepoFacts(signals, lang)`, borrar `DIR_NOTES` local en favor de `UI[lang].dirNotes`)
- Modify: `packages/cli/src/core/templates.ts` (títulos/cabecera desde `UI[lang]`; `renderRepoFacts(facts, lang)`; `renderClaudeMd/renderAgentsMd/renderCopilotInstructions(entries, facts, lang)` con `facts` opcional y `lang` obligatorio)
- Test: `packages/cli/test/repo-facts.test.ts`, `packages/cli/test/templates.test.ts`

- [ ] **Step 1: Test rojo** — actualizar llamadas existentes a la firma nueva con `"es"` (asserts intactos) y añadir:

```typescript
// repo-facts.test.ts
it("annotates structure in English when lang is en", () => {
  const dirs = extractStructure(baseSignals({ files: ["src/index.ts"] }), "en");
  expect(dirs).toEqual([{ dir: "src/", note: "source code" }]);
});

// templates.test.ts
it("renders facts section titles and header in English", () => {
  const output = renderClaudeMd(entries, facts, "en");
  expect(output).toContain("Generated by agent-rules-init");
  expect(output).toContain("## Repo commands");
  expect(output).toContain("## Structure");
  expect(output).not.toContain("Comandos del repo");
});
```

- [ ] **Step 2: Verificar rojo.**
- [ ] **Step 3: Implementación** — thread de `lang` por las funciones; la nota "…y N más{ en X}" via `UI[lang].andMore(count, file?)`.
- [ ] **Step 4: Verificar verde** (ambos archivos de test).
- [ ] **Step 5: Commit** — `feat(core): bilingual repo facts and templates`.

---

### Task 5: prompt-engine, llm-bridge y cableado final de cli.ts

**Files:**
- Modify: `packages/cli/src/core/prompt-engine.ts` (`collectLowConfidenceQuestions(detections, lang)` usa `UI[lang].question` + `UI[lang].fieldLabels`; `defaultPromptFn` recibe el aviso de TTY desde UI)
- Modify: `packages/cli/src/core/llm-bridge.ts` (`polishWithAssistant(assistant, content, execFn, lang)` con prompt desde `UI[lang].polishPrompt`)
- Modify: `packages/cli/src/cli.ts` (runCli acepta `lang?`, default `detectLang()`; todos los mensajes/USAGE desde `UI[lang]`; fallback sin stack desde `UI[lang].noStackFallback`; main() resuelve lang de la action)
- Test: `packages/cli/test/prompt-engine.test.ts`, `packages/cli/test/llm-bridge.test.ts`, `packages/cli/test/cli.test.ts`

- [ ] **Step 1: Test rojo**:

```typescript
// prompt-engine.test.ts — actualizar llamadas existentes con "es" y añadir:
it("builds human-readable questions in both languages", () => {
  const detections = [{ packId: "js-ts", language: "JavaScript", testRunner: { value: "unknown", confidence: "low" } }] as DetectionResult[];
  expect(collectLowConfidenceQuestions(detections, "es")[0].message).toBe(
    "No se pudo determinar el test runner para JavaScript. ¿Cuál usáis?"
  );
  expect(collectLowConfidenceQuestions(detections, "en")[0].message).toBe(
    "Couldn't determine the test runner for JavaScript. Which one do you use?"
  );
});

// cli.test.ts — e2e:
it("generates fully English output with lang en", async () => {
  const promptFn = vi.fn().mockResolvedValue("");
  await runCli(tmpDir, { promptFn, skipLlm: true, lang: "en" });
  const claudeMd = fs.readFileSync(path.join(tmpDir, "CLAUDE.generated.md"), "utf-8");
  expect(claudeMd).toContain("Generated by agent-rules-init");
  expect(claudeMd).not.toMatch(/Proyecto|Comandos del repo|Ejecuta los tests/);
});
```

- [ ] **Step 2: Verificar rojo.**
- [ ] **Step 3: Implementación** — thread completo; `RunCliOptions` gana `lang?: Lang`; `main()` pasa `action.lang` y usa `UI[lang]` para intro/outro/avisos; `invalid-lang` imprime `UI[detectLang()].invalidLang(value)` + usage y exit 1.
- [ ] **Step 4: Verificar verde** — suite completa `npm test --workspaces --if-present`.
- [ ] **Step 5: Commit** — `feat(cli): full bilingual pipeline with --lang and locale detection`.

---

### Task 6: Documentación, versión y validación final

**Files:**
- Modify: `docs/writing-a-pack.md` (contrato nuevo con `lang`, patrón `TEXTS`)
- Modify: `README.md` (inglés como idioma principal + sección "En español" breve; documentar `--lang`, `--help`, `--version` y las secciones de RepoFacts)
- Modify: `packages/cli/package.json` (`version: "0.3.0"`)

- [ ] **Step 1:** actualizar `writing-a-pack.md`: firma `rules(detection, lang)`/`promptTemplates(detection, lang)`, obligación de tabla `TEXTS: Record<Lang, ...>` con ambos idiomas y de usar los constructores compartidos de `core/i18n.ts`, y que los tests del pack cubran ambos idiomas.
- [ ] **Step 2:** reescribir `README.md` en inglés (mismo contenido: qué es, uso, tabla de stacks, secciones generadas incluyendo repo facts, flags, desarrollo local, contribuir, licencia) con sección corta en español al final que enlaza lo esencial.
- [ ] **Step 3:** bump `0.2.1` → `0.3.0` en `packages/cli/package.json` (feature nueva: repo facts + i18n) y `npm install` para refrescar el lockfile.
- [ ] **Step 4:** `npm test --workspaces --if-present` (todo verde) y `npm run build --workspaces --if-present`.
- [ ] **Step 5: Validación manual** — repo sintético temporal: `node dist/bin.js --lang en` (contenido inglés), `node dist/bin.js --lang es` en carpeta limpia (español), `node dist/bin.js --lang xx` (exit 1 + usage), `node dist/bin.js --version` (0.3.0). Borrar el repo temporal.
- [ ] **Step 6:** actualizar DEVNOTES.md (sección i18n + estado de publicación 0.3.0 pendiente de `npm publish` manual).
- [ ] **Step 7: Commit** — `docs: english-first README, pack contract update and 0.3.0 bump`.
